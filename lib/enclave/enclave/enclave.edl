enclave {
    trusted {
        public void test();
		public int enclave_f([in, count=s] float *m, int s, [out] int *label);
    };

    untrusted {
        void ocall_stdout_string([in, string] const char *str);
        void ocall_stderr_string([in, string] const char *str);
    };
};
